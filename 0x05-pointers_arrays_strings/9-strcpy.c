#include"main.h"
/**
 * *_strcpy
 * @dest: destination value
 * @src: source value
 *
 * Return: pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	int i = -1;

	do {
		i++;
	
		dest[i] = scr[i];

	} while (scr[i] != '\0');

return (dest);
}
