#include"main.h"
/**
 * *_strcpy
 * @dest: destination value
 * @src: source value
 *
 * Return: pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	int i = -1;
	
	for (i = 0; scr[i] != '\0'); i++)

        {
                dest[i] = scr[i];
        }
	dest[i++] = '\0';
		
	return (dest);
}
