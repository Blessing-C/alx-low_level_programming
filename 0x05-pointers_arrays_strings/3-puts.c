#include "main.h"

/**
  * _strlen - Prints a string
  * @s: The string to print
  *
  * Return: Returns length of the string
  */
int _strlen(char *s)
{
	for (; *str != '\0'; str++)
	{
		_putchar(*str);
	}
	_putchar('\n');
}
